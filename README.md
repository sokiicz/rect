# rect

> Status: wip — created 2026-06-19

## Setup
```bash
pnpm install
pnpm dev
```

## Env vars
See `D:/ai/Apps/Secrets/rect.txt` (local only, not committed).

## Deploy
GitHub Pages — set `base: '/rect/'` in `vite.config.ts`, push, enable Pages on `gh-pages` branch.
